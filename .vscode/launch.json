{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run ESLint (npm run lint)",
            "runtimeExecutable": "npm",
            "runtimeArgs": [
                "run",
                "lint"
            ],
            "cwd": "${workspaceFolder}",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}